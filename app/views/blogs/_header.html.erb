<header>
    <nav class="navbar navbar-light bg-light p-3">
        <div class="container-fluid, mt:5">
            <div class="col-md-6"><h4>Instagram_App_Clone</h4></div>

            <div class="col-md-3">
                <%= link_to 'Blogs_list', blogs_path , class: "btn btn-info text-light"%>
                <%= link_to 'New Blog', new_blog_path , class: "btn btn-warning text-light"%>
            </div>
                <% if logged_in? %>
            <div class="col-md-3">
                <%= link_to "Profile", user_path(current_user.id) , class: "btn btn-primary text-light"%>
                <%= link_to "Logout", session_path(current_user.id), method: :delete , class: "btn btn-danger text-light" %>
                <% else %>
                <%= link_to "Sign up", new_user_path , class: "btn btn-primary text-light" %>
                <%= link_to "Login", new_session_path, class: "btn btn-success text-light" %>
            </div>
            <% end %>
        </div>
    </nav>
</header>